## AI Agent

LLM是懂很多知识的专家
AI Agent是能够帮你干活的数字员工/助理(你给目标，它执行)

```
AI Agent = LLM + Planning + Memory + Tool Use 
```

## Prompt(提示词)

发给LLM的文本指令

## Embedding(嵌入)

在AI中可等价理解为 Vectorization(向量化)，是因为它的最终表现形式就是一组连续的向量化数值